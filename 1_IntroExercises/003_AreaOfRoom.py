#!/usr/bin/env python3

width = float(input('Enter the width of your room in feet: '))

length = float(input('Enter the length of your room in feet: '))

area = width * length

print(f'The area of your room is {area:.1f} square feet.')
